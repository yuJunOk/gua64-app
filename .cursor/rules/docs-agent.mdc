---
description: 以 docs/AGENT.md 为权威开发指南，会话中按需读取
alwaysApply: true
---

# 读取项目 Agent 文档

本仓库的 AI 协作约定以 **`docs/AGENT.md`** 为准。

在处理与本项目相关的实现、重构、代码评审或架构/规范类问题时：

1. **若上下文里还没有该文件的完整内容**，先用可用工具 **读取 `docs/AGENT.md`**，再作答或改代码。
2. **遵守** 其中的设计规范、技术栈偏好、代码组织与自检要求。
3. 若文档与用户当轮明确指令冲突，以用户指令为准；否则以 **`docs/AGENT.md`** 为准。

不要依赖旧摘要替代当前文件；规范变更以仓库内文件为准。
